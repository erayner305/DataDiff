# DataDiff
Accepts two data files and returns the line level difference

## Instructions
Massage the data files
- Ensure the column headers are the same
    - Remove unnecessary columns
    - 'Well', 'Date', 'Constituent' must be present
- Ensure the constituent/well/gradient names are the same
- Remove unnecessary flags
- Check for hidden sheets
